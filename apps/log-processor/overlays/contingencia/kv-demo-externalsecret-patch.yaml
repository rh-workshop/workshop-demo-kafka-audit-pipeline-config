# La llave de cifrado de contingencia. Cada ambiente lee la suya del almacen.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kv-demo
spec:
  data:
    - secretKey: aes-key
      remoteRef:
        key: platform/kafka-audit/contingencia
        property: aes-key
